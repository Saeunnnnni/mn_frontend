import React from 'react';

const ImageCategory = () => {
    return (
        <div>
            
            <button
                    onClick={onSearch}
                    id='CategoryBtn'>
                </button>
        </div>
    );
};

export default ImageCategory;